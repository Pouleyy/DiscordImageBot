{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "API",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 5858,
            "restart": false,
            "sourceMaps": true,
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/app",
            "outDir": "${workspaceRoot}/dist"
        }
    ]
}